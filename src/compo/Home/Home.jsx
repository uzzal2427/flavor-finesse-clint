import React from 'react';
import { Card, Navbar } from 'react-bootstrap';
import Header from '../Navbar/Header';

const Home = () => {
    return (
        <div>
            <Header></Header>
        </div>
    );
};

export default Home;